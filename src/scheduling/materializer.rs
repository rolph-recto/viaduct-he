use std::collections::HashMap;

use crate::lang::{ArrayTransform, OffsetMap};

pub struct ArrayMaterializer {}

impl ArrayMaterializer {
    pub fn new() -> Self {
        ArrayMaterializer { }
    }
}